apply plugin: "application"

mainClassName = "org.janelia.jacsstorage.service.StorageAgentListenerLauncher"
applicationName = "storageListener"

project(":jacsstorage-services") {
    dependencies {
        api project(":jacsstorage-core")

        implementation CDI_SE_CORE_LIB,
                       CMDLINE_ARG_LIB,
                       JAX_RS_CLIENT_LIB,
                       JERSEY_MEDIA_JSON_LIB,
                       MONGODB_DRIVER_LIB,
                       REFLECTIONS_LIB
    }
}

ext.moduleName = 'org.janelia.jacsstorage.services'
