project(":jacsstorage-services") {
    dependencies {
        implementation(project(":jacsstorage-core")) {
        }

        implementation(JAX_RS_CLIENT_LIB) {
        }

        implementation(JERSEY_MEDIA_JSON_LIB) {
        }

        implementation(REFLECTIONS_LIB) {
            exclude group: "com.google.code.findbugs"
        }

        implementation(MONGODB_DRIVER_LIB) {
            exclude group: 'org.mongodb', module: 'bson'
            exclude group: 'org.mongodb', module: 'mongodb-driver-core'
        }

        implementation(MONGODB_DRIVER_CORE_LIB) {
            exclude group: 'org.mongodb', module: 'bson'
        }

        implementation MONGODB_BSON_LIB
    }
}

ext.moduleName = 'org.janelia.jacsstorage.services'
