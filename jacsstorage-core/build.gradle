project(":jacsstorage-core") {
    dependencies {
        compile project(":jacsstorage-api"),
                CDI_API_LIB,
                COMMONS_COMPRESS_LIB,
                MESSAGEPACK_LIB
    }
}
