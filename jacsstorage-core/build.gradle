project(":jacsstorage-core") {
    dependencies {
        api project(":jacsstorage-api"),
            JAKARTA_CDI_API_LIB

        implementation JWT_API_LIB

        runtimeOnly JWT_IMPL_LIB,
                    JWT_JACKSON_LIB
    }
}

ext.moduleName = 'org.janelia.jacsstorage.core'
