apply plugin: 'application'

applicationName = 'storageClientApp'
mainClassName = 'org.janelia.jacsstorage.client.StorageClientApp'

project(":jacsstorage-utils") {
    dependencies {
        implementation project(":jacsstorage-core"),
                       BENCHMARK_CORE_LIB,
                       BENCHMARK_ANN_PROCESSOR_LIB,
                       CDI_API_LIB,
                       CDI_SE_CORE_LIB,
                       CMDLINE_ARG_LIB,
                       JAX_RS_CLIENT_LIB,
                       JERSEY_INJECT_LIB,
                       JERSEY_MEDIA_JSON_LIB
    }

    task createStorageClientApp(type: CreateStartScripts) {
        mainClassName = "org.janelia.jacsstorage.client.StorageClientApp"
	classpath = startScripts.classpath
	outputDir = startScripts.outputDir
	applicationName = "storageClientApp"
    }

    task createStoragePersistAvgBenchmark(type: CreateStartScripts) {
        mainClassName = "org.janelia.jacsstorage.benchmarks.StoragePersistAvgBenchmark"
	classpath = startScripts.classpath
	outputDir = startScripts.outputDir
	applicationName = "storagePersistAvgBenchmarks"
    }

    task createStoragePersistThroughputBenchmark(type: CreateStartScripts) {
        mainClassName = "org.janelia.jacsstorage.benchmarks.StoragePersistThroughputBenchmark"
	classpath = startScripts.classpath
	outputDir = startScripts.outputDir
	applicationName = "storagePersistThroughputBenchmarks"
    }

applicationDistribution.into("bin") {
        duplicatesStrategy = DuplicatesStrategy.EXCLUDE
        from(createStorageClientApp)
        from(createStoragePersistAvgBenchmark)
        from(createStoragePersistThroughputBenchmark)
        fileMode = 0755
    }

}

ext.moduleName = 'org.janelia.jacsstorage.utils'
